arrayPessoas = [[1], [2], [3], [4], [5]]

for (var i = 0; i < arrayPessoas.lenght; i++){
    var pessoa = arrayPessoas[0]
    
}


function obtencaodedados(pessoaA) {
    dadoIdade = prompt("Idade:")
    var pessoaA = pessoa.push(dadoIdade)
    dadoNome = prompt("Nome:")
    var pessoaA = pessoa.push(dadoNome)
    dadoAltura = prompt("Altura")
    var pessoaA = pessoa.push(dadoAltura)
    dadoPeso = prompt("Peso:")
    var pessoaA = pessoa.push(dadoPeso)
    dadoSexo = prompt("Sexo: (M ou F)")
    var pessoaA = pessoa.push(dadoSexo)
}
